const config = {
  siteName: 'Quirky Creatures', // used in homepage title metatag
  authorName: 'Mark Mitchell',
  description: 'Illustrated cards of quirky animal facts!', // used in homepage description metatag
  dev: {
    static: './static',
    outDir: './public'
  }
};

module.exports = config;
